package main

// func say(s string, w *sync.WaitGroup) {
// 	fmt.Println(s)
// 	w.Done()
// }

// func sayWithData(s string, data string, ch chan string) {
// 	fmt.Println(s)
// 	ch <- data
// }

func gorouthineEx() {
	// fmt.Println("start")

	// var wait sync.WaitGroup
	// wait.Add(3)

	// go say("async 1", &wait)
	// go say("async 2", &wait)
	// go say("async 3", &wait)

	// wait.Wait()

	// ch := make(chan string)
	// go sayWithData("hello", "world", ch)

	// fmt.Println(<-ch)
}
