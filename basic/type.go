package main

func typeEx() {
	// 타입변환

	// 타입변환시 int -> uint으로 변환할때는 암묵적 변홚이 이루어 지지 않기 때문에
	// 명시적으로 지정해줘야한다.

	// var i uint = 100
	// i = uint(1)

	// fmt.Println(i)

	// str := "hello world"
	// bytes := []byte(str)
	// str2 := string(bytes)

	// fmt.Println(str)
	// fmt.Println(bytes)
	// fmt.Println(str2)

	// 포인터 주소 저장

	// a := 1
	// b := &a
	// fmt.Println(a, b)
}
